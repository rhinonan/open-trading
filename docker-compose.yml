services:
  open-trading:
    build: .
    container_name: open-trading
    ports:
      - "3003:3003"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    mem_limit: 2g
    cpus: 2
    restart: unless-stopped
